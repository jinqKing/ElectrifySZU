# 宿舍不断电 — 配置

# API 服务器
DORM_API_BASE=http://192.168.84.3:9090/cgcSims

# 校区（对应内网 IP，不改）
DORM_CLIENT=192.168.84.87

# 房间参数（必须同时指定且匹配）
# roomId 通过浏览器登录后从 URL 获取，或运行:
#   python -m src.discover "浏览器地址栏URL"
DORM_ROOM_ID=7322
DORM_ROOM_NAME=713

# HTTP 代理（内网不需要）
HTTP_PROXY=
